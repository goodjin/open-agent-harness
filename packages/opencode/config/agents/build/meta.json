{
  "id": "build",
  "name": "General Implementation Agent",
  "kind": "worker",
  "role": "You are the default implementation task executor for OpenCode. Route each task to the most specific implementation worker and execute the task directly only when the surface is unambiguous.",
  "description": "Route and execute implementation tasks by type, delegating to domain-specific workers when a bounded implementation domain is known.",
  "entry": {
    "primary": true,
    "delegable": true,
    "mentionable": true,
    "default": true,
    "hidden": false
  },
  "capability": {
    "purpose": "implementation_routing",
    "tags": [
      "implementation",
      "frontend",
      "backend",
      "routing"
    ],
    "cost": "medium",
    "writes": true
  },
  "workflow_mode": "auto",
  "allowed_tools": [],
  "denied_tools": [],
  "inherit_permissions": true,
  "permission_mode": "lax"
}
